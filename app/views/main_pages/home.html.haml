%h1 RecipeShare
%p
	Search for your favourite recipes or share your own recipes with others

= form_tag("/", method: "get") do
	= label_tag :search
	= text_field_tag :search
	= submit_tag "Search"


= render partial: 'shared/recipe_list', locals: { recipes: @recipes, my_recipes: false  }
